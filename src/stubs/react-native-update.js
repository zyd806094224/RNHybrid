import React from 'react';

// Stub for react-native-update on harmony platform (Turbo Module not available)

export class Pushy {
    constructor() {}
}

export function UpdateProvider({ children }) {
    return children;
}
